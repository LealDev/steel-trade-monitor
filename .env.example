# Copie para .env e preencha. O .env está no .gitignore.
# Senha do Postgres de produção
POSTGRES_PASSWORD=
# Token exigido no header X-Ingestion-Token para disparo manual de coleta
# (gere um: openssl rand -hex 24)
INGESTION_TOKEN=
